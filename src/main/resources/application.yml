server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/accounting_socks
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: none
  liquibase:
    enabled: true